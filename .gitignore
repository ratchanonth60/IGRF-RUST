/target/
/logs/*.csv
/sensor_log.csv
/SystemConfig.json
*.json.tmp
.DS_Store

# Space-Track credentials for "Update TLE Info". Committed template: .env.example
/.env
# TLEs fetched from Space-Track at runtime (not redistributable).
/tle_data.db
/tle_data.db-journal
/tle_data.db-wal
/tle_data.db-shm

# Controller firmware dump. Kept out of the repo deliberately: this is a public
# mirror and the dump is a full flash image of the STM32. What was learned from
# it is written up in docs/controller-protocol.md instead.
/test.bin
__pycache__/
